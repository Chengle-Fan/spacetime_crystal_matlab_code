# V3.0 验收报告与修复建议

> 范围说明：第 1–7 节保留的是旧 V3.0 `run_fdtd.m`、Case A/B/C 与早期二维 FFT 方案的历史验收记录，其中“当前实现”和“重新验收门槛”不再描述现工作树。用户随后收窄并修正了需求；当前 PWE、TMM、FDTD/FFT 契约分别以第 8、9、10 节为准。

## 1. 验收结论

**结论：V3.0 整体验收不通过，建议完成 P0/P1/P2 修复并重新验收后发布 3.0.1。**

本次共整理 **2 项 P0、11 项 P1、9 项 P2、6 项 P3**，另列 4 项尚需专项验证的风险。P0 来自验收过程中用户明确补充的有限样品实验口径。

三个默认入口在 MATLAB R2026a 下均能完整运行，默认演示和原计划中的周期体材料 FDTD-FFT/TMM 数值阈值也已通过；但公开函数仍存在可复现的错误结果、CFL 假安全、无效输入产生伪数据，以及计划明确要求的输出字段缺失。因此，目前只能判定“默认脚本执行完成”，不能判定 V3.0 公共 API 和通用科研算例通过。

验收过程中，用户进一步明确：**FDTD 必须模拟实际的有限尺寸材料样品；能带重建必须使用样品中采集的电场 `E(x,t)`。** 这一明确要求优先于计划文档原先的“周期边界 + `D(x,t)` FFT”方案。按新验收口径，Case B/C 是周期环形体材料，Case A 虽有有限计算域却仍由调制材料铺满全域，三个公开 Case 都不是有限材料样品；FFT 输入又是 `D`，属于架构级不符合。原有 `0.0272/0.2394` 指标只能移作内部周期体算法回归，不能作为公开 FDTD 或有限样品实验仿真的验收结论。

本报告把 `V3_REFACTOR_PLAN.md` 作为产品规格和验收基准。文件中“本次只创建计划文档”等文字是重构计划形成时的历史约束，不是本次验收指令；本次实际执行以用户提出的发布验收和生成 repair 文档为准。

验收对象：

- 分支：`V3`
- 提交：`db56a79`（`V3.0发布`）
- 版本：`3.0.0`
- 日期：2026-08-29
- 实跑环境：macOS arm64，MATLAB `R2026a Update 3`
- 最低声明环境 MATLAB R2020a 未安装，故不能声称已经在 R2020a 实跑通过

## 2. 已执行验收与结果

| 验收项 | 结果 | 证据或说明 |
|---|---|---|
| Git 跟踪树扁平化、旧目录和旧入口移除 | 通过 | 跟踪文件与 V3 目标树一致；本报告 `repair.md` 是验收后按用户要求新增的交付物 |
| 旧版恢复标签 | 通过 | `legacy-pre-v3-refactor` 存在并可定位重构前提交 |
| `VERSION.txt=3.0.0`、README、相位和归一化说明 | 通过 | 文档已更新 |
| 无 `addpath`、startup、旧函数依赖和默认缓存 | 通过 | Git 跟踪的 `.m` 文件中未发现相关调用；三个 `doSave` 默认关闭 |
| MATLAB Code Analyzer | 通过（有非阻断提示） | 目标文件无语法错误；提示主要是固定 `doSave=false` 导致的不可达保存块、未用变量和性能建议 |
| `run_pwe` 直接运行 | 通过（R2026a） | 默认输出 `median |PWE-TMM|/Ω=0.002001`；两处动量带隙中心正常 |
| `run_tmm` 直接运行 | 通过（R2026a） | 找到两个默认动量带隙并完成绘图、输出 |
| `run_fdtd` 直接运行 | 通过（仅原周期体回归） | Case A/B/C 均执行完成；116 个通带 k 列上中位 `|ΔωT|=0.0272`、最大值 `0.2394`，分别满足原计划 `0.10/0.30` 阈值 |
| 有限尺寸样品实验几何 | 失败 | Case B/C 使用周期环；Case A 虽有海绵边界，材料也铺满计算域。三个 Case 都没有有限材料样品、外部背景区和完整实验式源/测量几何 |
| 实验可测电场 FFT | 失败 | `fdtd_fft_bands` 和两个能带 Case 均传入 `D`，不是用户明确要求的 `E` |
| 均匀介质 PWE/TMM 解析色散 | 通过（解析/静态核对） | 两种 PWE 模式和 TMM 均恢复 `ω=±k/sqrt(εμ)` |
| FDTD 时间界面对齐和显式中心修正 | 部分通过 | 对齐与中心修正路径已执行；尚未用有/无修正对照和收敛阶独立验证其数值效果，且 CFL/材料合法性防线不完整 |
| 一般 `ε(x,t)` 双重折叠 | 失败（作为验收图） | 默认 Case C 虽执行完成，但只有 8 个折叠 k 点、8 个真实时间周期，图中无可辨认脊线；奇数网格和函数头声称支持的重复空间端点还会产生错误结果 |
| PWE Fourier 差阶完整覆盖 | 失败 | 自定义较小 `Nx/Nt` 时发生静默混叠 |
| PWE 双模式输出契约 | 部分通过 | 两模式可求解；`mode='k'` 缺中心谐波权重 |
| TMM 输出契约 | 部分通过 | 缺计划要求的单周期矩阵 `U` |
| FDTD CFL 严格检查 | 失败 | 可报告稳定但实际 Courant 数远大于 1 |
| FFT 不隐式调用理论求解器、明确不能恢复 `Im(ω)` | 通过 | 实现和 README 均符合 |
| Base MATLAB / R2020a+ | 部分验证 | R2026a 无 Toolbox 依赖运行成功；R2020a 兼容性尚未实跑 |

## 3. 已确认缺陷

优先级定义：

- **P0**：验收目标或仿真架构不符合明确需求，现有结果不能用于声明的科研用途；阻断发布。
- **P1**：会产生错误科研结果、伪安全或伪记录；发布修复前必须处理。
- **P2**：公开契约缺失、适用范围明显错误或重要鲁棒性缺陷；建议与 P1 一并修复。
- **P3**：低风险契约、兼容性或诊断问题。

### P0-01：所有公开 FDTD 算例都不是有限尺寸材料样品仿真

- 位置：`run_fdtd.m:87-121,169-207,277-305`
- 当前实现：Case B/C 都设置 `boundary='periodic'`，调制材料覆盖整个计算域。它们模拟的是无边界的周期环形体材料，不包含有限样品的入射面、出射面、背景介质、有限尺寸共振或样品边界散射。Case A 虽使用海绵外边界，但材料仍铺满计算域，也不能等同于“有限材料样品”。
- 影响：当前结果不能作为实际有限样品实验的直接仿真；有限长度引起的谱线展宽、Fabry-Pérot 模式、界面反射/透射和测量窗口效应全部缺失。还需明确：有限样品的空间-时间 FFT 得到的是**源加权的有限尺寸响应谱** `|E(k,ω)|^2`，不是严格的无限体本征能带；只有经过长度、ROI、窗和网格收敛后稳定的 ridge，才能作为 bulk-band 候选。
- 修复建议：把能带 FDTD 几何改为真正的有限样品：

  ```text
  海绵 | 背景区 | 宽带源 | 背景区 | 有限调制样品 | 背景区 | 海绵
                                [样品内部 E(x,t) 测量 ROI]
  ```

  - 外边界使用海绵并留足背景缓冲区；海绵仍应明确为简化吸收层而非 PML。固定样品、源和 ROI，只改变背景 buffer/海绵参数做外边界收敛，并增加无样品 reference run 量化残余反射。
  - `epsFun(x,t)` 只在样品区内调制，样品外保持背景材料，从而产生真实的有限样品界面。空间晶体样品取整数个原胞并保持相同 termination/调制相位，界面落在一致的 Yee 网格位置；纯时间晶体则按固定物理长度定义样品。
  - 不能把当前 `sourceD` 直接当成已归一化物理电流：它是每一步加到 D 的增量。应新增 `Jsource` 并由内核按 `dt`（空间 delta 还要按 `dx`）缩放，或明确约定 `sourceD=dt*J`。实验式单侧入射优先使用匹配的 D/B 成对源或 TFSF；至少做无样品参考来校正入射谱、分离反射，并对非互易样品做左右入射。
  - 在样品内部采集 `E(x,t)`；空间晶体 ROI 避开端面整数个原胞，纯时间晶体则按波长或固定物理距离避开端面。同时保留反射/透射探针用于实验可比验证。
  - 用至少三种样品长度做两类收敛：其一固定中央 ROI 增大样品以隔离边界，其二随样品增大 ROI 并把响应插值到共同的 signed-k 网格，按 `1/L` 外推 ridge 包络/中心和线宽。另行完成 `dx/dt` 网格收敛。

  Case A 也应改成“背景区中的波包入射有限调制样品并显示场分布”。若仍保留全域均匀材料的波包，它只能作为内部内核回归，必须从公开实验算例和总体验收中剥离，不能参与 `ALL CHECKS PASSED`。

### P0-02：能带 FFT 使用 `D(x,t)`，不符合实验电场采样要求

- 位置：`fdtd_fft_bands.m:1-2,25-29`、`run_fdtd.m:200-201,304-305`
- 当前实现：FFT 公共函数把输入定义为 `D`，Case B/C 也传入 `bandField.D`。用户明确要求模拟实验采集的电场，因此验收观测量必须是 `E(x,t)`。
- 影响：在理想无限周期线性系统中，D/E 通常共享相同 Floquet 极点，但 `D=εE` 会改变材料谐波权重；在有限样品的边界、窗函数和多峰叠加下，ridge 选择也可能不同。更根本的是用户指定的实验观测量就是 E，因此 D 不能替代验收数据。
- 对照：在当前周期 Case B 上事后改用同一仿真的 `E` 做 FFT，得到中位误差 `0.0268370`、最大误差 `0.2393662`，仍满足旧阈值；说明切换实验观测量本身不会破坏该底层回归。但 Case C 的 E-FFT 动态范围仍只有 `2.153 dB`，证明仅把 D 换成 E 不能修复有限样品架构和欠分辨问题。
- 修复建议：把函数入参改为通用 `fieldData` 并明确 `fieldComponent='E'`，默认且验收强制传入 `bandField.E`。`D` 可作为可选诊断对照，但不得再作为“实验电场能带”的验收数据。输出中记录 E 位于整数 Yee 空间/时间节点、理想时间突变处的取侧约定、测量 ROI、空间/时间窗和归一化方式；同时保留 raw signed `(k,ω)` 响应谱，并把 Floquet 折叠做成可配置附加视图。Case B 只折 `ω`、保留 signed k；Case C 仅在整数晶格 ROI 上双折叠。先用 raw 谱审计源/边界，再用 folded 谱对比体理论。

### P1-01：PWE 自定义采样数过小时发生静默 Fourier 混叠

- 位置：`pwe_fourier.m:47-56,61-75,101-110`
- 复现：

  ```matlab
  cfg = struct('Lambda',1,'T',1,'Nspace',1,'Mtime',0,'Nx',1,'Nt',1);
  F = pwe_fourier(@(X,Time) ones(size(X)), [], cfg);
  F.epsilon
  ```

- 实际：常数介质的 `n=-2:2` 五个系数全部为 `1`；正确结果应只有 `n=0` 为 `1`。另一个 `Nspace=2,Nx=4` 用例中，`epsCoeff(0,4)` 也错误返回约 `1`。
- 影响：卷积矩阵错误或秩亏，可进一步产生伪能带、`Inf/NaN`。
- 修复建议：验证 `Nspace/Mtime` 为非负有限整数，`Nx/Nt` 为正有限整数，并至少满足 `Nx >= 4*Nspace+1`、`Nt >= 4*Mtime+1`。该下界只防止请求的差阶彼此发生离散模碰撞，不能防止材料真实的 Nyquist 外高次谐波混入；仍需保留过采样和 `Nx/Nt` 加倍收敛检查。常数介质的所有非零阶系数应作为回归检查。

### P1-02：默认 CFL 审计会漏掉时域和 H 半格上的最高波速

- 位置：`fdtd1d.m:185-212,292-309`
- 原因：

  1. 未提供 `maxWaveSpeed` 时只在 9 个 `linspace` 时刻采样；窄的高速时间区间可能完全落在采样点之间。
  2. 审计在 `muFun(x,t)` 的 E 网格上取样，但实际推进在 `muFun(xH,tHalf)` 的 H 半格上使用材料。

- 实测一：取 `dx=0.1,dt=0.05`，令 `ε(t)=1-0.99*exp(-((t-0.2)/0.005)^2)`。九点审计漏过 `t=0.2` 的窄谷，报告 Courant 数 `0.5`，实际推进网格上的峰值为 `5`，仍继续计算。默认 Case B 也存在同类低估：九个审计时刻为 `0,3,...,24T`，全部落在 `ε=4` 层，报告 `0.2`，而 `ε=1` 层的真实最坏值是 `0.4`；默认仍稳定，但审计值已低估一倍。
- 实测二：令 E 网格上 `μ=1`、H 半格上 `μ=0.01`，函数报告 `0.5`，实际 H 网格 Courant 数为 `5`，四步后 `max|E|` 约为 `2.7e4`。
- 影响：`sampledCourant<1` 不能证明稳定，且可能把已经数值发散的结果标为安全。
- 修复建议：

  - 对可解析或分段材料，要求入口提供经过验证的全局 `maxWaveSpeed` 上界。
  - 若继续自动审计，必须覆盖实际推进使用的 E/H 网格、全部推进节点/半节点及每个时间界面两侧；“九点采样”只能作为诊断值，不能作为严格 CFL 保证。
  - 默认入口应显式传入保守的已知上界；对 staggered 材料可用覆盖实际 E/H 网格和全时间的 `1/sqrt(min(ε)*min(μ))`，不能只在同一采样点计算乘积。

### P1-03：`maxWaveSpeed` 未验证，可直接绕过 CFL

- 位置：`fdtd1d.m:185-212`
- 实测：`maxWaveSpeed=-1` 被接受并返回负的 `sampledCourant`；即使实际 `dt/dx` 已超限也不会报错。
- 修复建议：要求其为有限、实数、严格正的标量；同时要求 `dx>0`，并在报错信息中区分“调用者提供的上界”和“抽样估计值”。

### P1-04：`recordEvery` 非整数时会返回伪造的零记录

- 位置：`fdtd1d.m:86-90,244,312-319`
- 实测：`recordEvery=1.5` 被接受，可返回形如 `t=[0,0.15,0]` 的时间数组，最后一行场历史是未写入的预分配零值；`recordEvery=0` 则在数组分配阶段以底层错误退出。
- 影响：输出尺寸看似有效，但包含并不存在的快照，可能污染后续 FFT 或统计。
- 修复建议：在任何分配前要求 `recordEvery` 为正有限整数。另需明确 `nSteps` 不能整除时是省略最终态还是额外记录最终态，并据此计算精确的记录索引。

### P1-05：FDTD 本构、初始场和源回调缺少统一的尺寸与有限性校验

- 位置：`fdtd1d.m:173-180,188-206,268-280,293,305-309`
- 已确认问题：

  - `eps=0` 或 `mu=0` 可使 CFL 结果变为 `NaN`，随后继续推进并输出 `NaN/Inf`。
  - 时间界面处的错误文字声称检查“finite nonzero”，实际只检查 `==0`，`NaN` 可以通过。
  - 多处只检查 `numel` 而不检查 `size`；返回列向量时会触发隐式扩展，最终在不相关位置报错。实测列向量材料回调触发 `MATLAB:nonLogicalConditional`，而不是清晰的输入错误。
  - `sourceD` 没有尺寸和有限性检查；`E0/Hhalf0` 已被展平并检查元素数，但矩阵也会被静默展平，且两者没有有限性检查。

- 修复建议：建立统一的材料/源求值 helper，每次调用后强制结果与查询网格 `size` 完全一致且全部有限；`ε/μ` 至少必须有限、实数且非零。若 3.0.1 决定只支持普通正介质，应同步收紧 README/API 并要求正实数；否则必须为负参数定义稳定性策略。源和初始场可为复数，但必须是有限向量。

### P1-06：重复空间端点是在 FFT 之后错误删除的

- 位置：`fdtd_fft_bands.m:122,157-166`
- 原因：代码先对含重复物理端点的 `D` 做长度为 `Nx` 的空间 FFT，之后才截掉 `powerFold(:,end)`；此时删除的是一个频率列，并不等价于在物理域删除重复样本。
- 实测：同一个场分别采用无重复端点和重复端点输入，所得功率谱最大绝对差达到 `0.6874517`。
- 修复建议：在任何空间 FFT 前完成端点判定；确认重复端点后先删除 `fieldData(:,end)` 和 `x(end)` 并更新 `Nx`，必要时校验首末场值在容差内相等，然后再构造 FFT 和 k 网格。no-fold 模式无法仅凭现有参数可靠判断端点语义，应规定只接受 endpoint-free 网格，或新增明确的 `hasRepeatedSpatialEndpoint/domainLength` 配置。

### P1-07：时间 Floquet 折叠依赖奇偶性，合法输入会错移或被拒绝

- 位置：`fdtd_fft_bands.m:134-152`
- 实测：四个完整周期、每周期 5 个样点、零填充因子 4 时，`ωT=1.25664` 的纯模态被折到 `-1.96350`；每周期 6 个样点的对照落在正确的最近 FFT 网格点 `1.17810`。
- 另一问题：当 `nFold` 为奇数时，合法的整数周期/无重复端点输入被 `mod(nFold,2)~=0` 直接拒绝，而规格没有偶数限制。
- 原因：对已经 `fftshift` 的频谱直接 reshape，并再次固定 `fftshift`，其分块原点在奇数个每周期样点时错半个 Floquet 区。
- 修复建议：在未移位的 FFT bin 上按模 `nFold` 显式累加谐波副本，最后只做一次中心排列；索引使用 `-floor(N/2):ceil(N/2)-1`，并增加奇偶周期数、奇偶每周期采样数和不同零填充因子的组合回归。

### P1-08：奇数个空间周期时折叠 k 网格偏移半格

- 位置：`fdtd_fft_bands.m:171-180`
- 实测：三个空间周期时输出 `[-π,-π/3,π/3]`，正确网格应为 `[-2π/3,0,2π/3]`；一个空间周期时唯一点甚至会被标成 `-g/2` 而不是 `0`。
- 修复建议：使用 `(-floor(nFoldK/2):ceil(nFoldK/2)-1)*(g/nFoldK)`，并确保功率列采用完全相同的排列。

### P1-09：逐 k 归一化缺少有效列阈值，会把浮点噪声放大成伪能带

- 位置：`fdtd_fft_bands.m:125-129,190-200`
- 实测：纯单 k 模态中，其他列的原始范数约 `1e-15`，目标列约 `45`，动态范围超过 `4e16`；由于代码只判断 `columnNorm==0`，最终所有测试列的最大功率都被归一到 `1`（0 dB）。
- 影响：未激发 k 列可能出现满强度伪谱和伪脊线。
- 修复建议：保留公开契约所需的逐 k 归一化，但先按全局最大列能量、机器精度和可配置实验噪声阈值生成 `activeKMask`；只归一化可靠列，低于阈值的列保持零且 ridge 置为 `NaN`。同时输出 `rawColumnPower`，便于科研审计。

### P1-10：有限样品 FFT 缺少空间测量窗和 ROI 契约

- 位置：`fdtd_fft_bands.m:119-136`、`run_fdtd.m:196-201,300-305`
- 当前实现：函数头把 x 定义成 periodic spatial grid，函数只施加时间 Hann 窗并直接对全部空间列做 FFT；入口也没有定义有限样品内部的测量 ROI。有限样品 ROI 实际应是均匀、endpoint-free 的开放测量窗，不是物理周期边界。
- 影响：有限样品两端、背景区、入射场和样品内部场会被混在同一次空间 FFT 中；硬截断会产生强 k 泄漏，有限尺寸共振也可能被误判为体能带。
- 修复建议：入口先明确选择样品内部 ROI，并记录其物理范围；需要按晶格折叠时，ROI 应使用无重复端点网格并跨越整数个内部晶格周期。时间窗应在脉冲到达后开始，使用无重复端点采样、覆盖整数个调制周期，并记录起止时刻和调制相位。FFT 配置增加可审计的空间/时间窗（如 Hann/Tukey/矩形）及 coherent gain、energy gain/ENBW。`2π/L_ROI` 和 `2π/tWindow` 只能称 DFT bin spacing，有效分辨率应由窗主瓣宽度/ENBW 给出。显示用逐 k 归一化谱和原始物理功率谱必须同时返回；只有 `activeKMask` 内且入射 reference 谱高于噪声阈值的 ridge 才参与理论比较。

### P1-11：Case C 欠分辨且 `ALL CHECKS PASSED` 构成验收假阳性

- 位置：`run_fdtd.m:69-82,217-237,277-339`
- 实测：`nSpatPerC=8,nTempPerC=8` 最终只产生 `32 x 8` 的折叠谱；真实频率分辨率 `Δ(ωT)=0.785398`，归一化 k 间隔为 `0.125`。整张谱的动态范围仅 `2.137 dB`，导出图呈 8 列大色块，没有可辨认的能带脊线。
- 影响：脚本唯一的量化断言是 Case B 的两个误差阈值，却在 Case A/C 没有方向性、反射、分辨率、脊线或理论对照的情况下打印 `ALL CHECKS PASSED`；这不能构成“一般 ε(x,t) 双重折叠谱”验收。
- 修复建议：有限样品架构确定后，至少把样品周期数和记录周期数提高到能满足预先声明的 `Δk/Δω`，在样品内部 ROI 对 E 加空间/时间窗，并显式报告谱尺寸和真实分辨率。旧周期几何的 24 空间周期 x 24 时间周期对照已把谱提高到 `96 x 24`、`Δ(ωT)=0.261799`、归一化 `Δk=0.041667`，动态范围由 `2.137 dB` 提高到约 `22.1 dB`，可作为资源量级参考，但仍需脊线收敛而不能只看 dB 范围。建议用入口显式调用 PWE 叠加或独立匹配主脊线；立即把结尾文字改为 `CASE B TMM BENCHMARK PASSED; Cases A/C generated without quantitative acceptance`，只有三个 Case 都有独立布尔验收后才能打印 `ALL CHECKS PASSED`。

### P2-01：TMM 缺少计划要求的 `result.U`

- 位置：`tmm_bands.m:55-79,81-90`；规格见 `V3_REFACTOR_PLAN.md:71`
- 实际：`U` 只存在于扫描循环的局部变量，结果结构没有该字段。
- 修复建议：预分配 `Uall=zeros(2,2,nK,'like',complex(0))`，每个 k 保存 `Uall(:,:,ik)=U` 并返回 `result.U=Uall`；同步更新函数头文档和最小示例。

### P2-02：TMM 无效物理输入可返回 `NaN/Inf` 伪结果

- 位置：`tmm_bands.m:37-53,61-78`
- 复现：`tmm_bands(1,[],1,[])` 不报错，而是返回 `Omega=Inf`、`omega=[NaN;NaN]`。零总周期、负持续时间、零 `ε/μ`、非有限 k 也缺少前置校验。
- 修复建议：要求非空层、各持续时间为非负有限实数且总周期严格为正、k 至少全部有限；`ε/μ` 至少为有限实数且非零。若产品把能带扫描限定为实 k 或只支持普通正介质，应把这一收窄同步写入 README/API。若未来支持复数或有耗材料，需另行定义 `gapMask`，不能沿用当前实半迹判据。

### P2-03：固定频率 `omega -> k` 模式缺少中心谐波权重

- 位置：`pwe_bands.m:159-195`；规格见 `V3_REFACTOR_PLAN.md:47`
- 影响：两个公开模式的输出不统一，固定频率结果无法使用同一物理权重筛选。
- 修复建议：在 `mode='k'` 分支复用固定 k 分支的权重计算并返回 `m0Weight`，形状统一为 `nOmega x 2S`；同时明确中心权重是全部 `m=0` 分量还是仅 `(m,n)=(0,0)` 分量。这是计划与实现的输出契约缺口，不是本征值方程错误。

### P2-04：PWE 返回本征矢的物理语义写错

- 位置：`pwe_bands.m:6-8,127-139,174-176`
- 证据：现有矩阵满足 `K E=(ωI+W)Cμ H`、`K H=(ωI+W)Cε E`，实际状态是 `[E;H]`；函数头却称 `R=[D;B]`，代码内部也把两块命名为 `E/H`。
- 影响：调用者按文档解释可选本征矢时会错误重构场量。
- 修复建议：保持已核对正确的方程并把公开文档统一为 `[E;H]`。若产品确实要求 `[D;B]`，则必须推导并重写本征矩阵，不能只改变量名。

### P2-05：`run_pwe` 对一般非互易材料只覆盖半个空间 BZ

- 位置：`run_pwe.m:34,59,69,73-74`
- 实际：扫描范围是 `0...g/2`。默认 standing-wave 材料具有 `x -> -x` 对称性，半区足以显示当前默认谱；但入口和 README 将它描述成面向一般时空材料的第一 BZ。
- 影响：换成行波调制等非互易材料后会漏掉负 k 半区。
- 修复建议：改为关于零对称的完整 BZ。若采用半开域，可把 `nK1` 改为偶数并使用 `-g1/2+(0:nK1-1)*g1/nK1`，以同时保留 `k=0`；也可使用明确处理等价边界点的闭区间。若产品只展示不可约半区，则必须修改注释、坐标和 README。

### P2-06：降序或非有限 x 网格可绕过 CFL

- 位置：`fdtd1d.m:48-57,208-212`
- 实测：严格降序的均匀 x 被接受，返回 `dx=-0.1`、负 Courant 数并继续推进，与 FFT 函数要求正递增网格的契约也不一致。
- 修复建议：最简单的统一方案是要求 x 为有限实向量、严格递增、`dx>0`，均匀性容差按网格尺度计算；若要支持降序坐标，则 CFL 必须使用 `abs(dx)` 并完整定义输出坐标约定。

### P2-07：海绵边界参数无验证，吸收层可静默关闭或变成增益层

- 位置：`fdtd1d.m:221-238`
- 实际：`spongeCells=NaN/负数` 可静默关闭吸收，负 `spongeStrength` 会令指数阻尼大于 1。
- 修复建议：`spongeCells` 要求非负整数且不超过合理的半域宽度；`spongeStrength` 要求非负有限实数。两侧海绵重叠时应报错或明确告警。

### P2-08：默认周期域高斯源在边界处不连续

- 位置：`run_fdtd.m:176-181,284-286`
- 实际：Case B/C 使用周期边界，但 `E0=exp(-(x/sigma)^2)` 以 `x=0` 为中心且没有周期回绕。实测两个算例的周期相邻端点场跳变 `|E0(1)-E0(end)|` 都约为 `1`；所谓“Gaussian source”实际包含一个网格边界阶跃和很强的高 k 成分。
- 影响：它虽然能宽带激发 k，却会引入非高斯的高频/高波数伪影，加重 Case B 的竖向宽带条纹和 Case C 的谱填充。
- 修复建议：在旧周期回归中，把高斯中心移到域中央，或用周期距离 `min(|x-xc|,L-|x-xc|)`。实测仅把 Case B 源移到中央后，旧基准保持 `median=0.0272040,max=0.2393662`，说明可以无损消除接缝。按 P0-01 改成有限样品后，应改用位于背景区的有限时宽 `sourceD` 脉冲，不再用周期域初始半高斯。

### P2-09：PWE 周期、截断和扫描参数缺少基本合法性验证

- 位置：`pwe_fourier.m:41-59`、`pwe_bands.m:54-75,109-114,159-163`
- 实际：`Lambda/T<=0`、负数或非整数截断、非有限扫描值，以及非标量逻辑开关均未被统一前置拒绝。负截断可形成空阶数表和空谱，零周期可把倒格矢或调制频率变为 `Inf`，错误往往延迟到矩阵或下标操作才出现。
- 修复建议：对两个公开 PWE 函数统一使用同一配置验证器：周期为正有限实标量，截断为非负有限整数，扫描向量必须非空且全部有限，逻辑选项为标量。若产品决定只允许实扫描值，应把该限制同步写入公开契约。错误信息应指出字段名和允许范围。

### P3-01：`pweCfg.Lambda/T` 的文档契约与实现不一致

- 位置：`pwe_bands.m:27,54-67`
- 实测：传入与 `fourier` 完全不同的 `Lambda/T` 仍被静默接受，实际计算使用 `fourier` 中的值；代码只检查 `Nspace/Mtime`。
- 修复建议：二选一：校验四个配置字段都一致；或从 `pwe_bands` 的必需配置中删除重复的 `Lambda/T`，明确以 `fourier` 为唯一数据源。

### P3-02：Fourier 系数 lookup 未验证整数阶

- 位置：`pwe_fourier.m:115-125`
- 实际：`epsCoeff(0.5,0)` 会越过自定义范围校验，最终以内置“非整数下标”错误退出。
- 修复建议：在索引前要求 `m/n` 为有限整数标量，给出与超范围错误一致的 API 诊断。

### P3-03：FFT ridge 输出方向与函数头文档相反

- 位置：`fdtd_fft_bands.m:43-44,195-200`
- 实际：文档声明 `nK x 1`，实现因末尾转置返回 `1 x nK`。
- 修复建议：删除末尾转置并统一为列向量；或者修改公开契约和所有入口，但不应继续两者不一致。

### P3-04：V3 仍保留被忽略的兼容字段 `storeD`

- 位置：`fdtd1d.m:35`、`run_fdtd.m:118,193,298`
- 实际：字段被“兼容接受”但完全不起作用，与计划“不保留旧接口、兼容入口”的约束冲突。
- 修复建议：从函数头和三个配置结构中删除 `storeD`；`D` 历史始终保存的行为直接写入输出契约。

### P3-05：no-fold 的 raw k 轴缺少有符号物理解释

- 位置：`fdtd_fft_bands.m:122-123,181-185`
- 实测：`dx=0.25` 时输入 `k=-π` 的纯模态，raw DFT bin 被数值标为 `21.9911=2π/dx-π`。两者在离散采样中互为别名，因而不属于功率计算错误；但直接把后者称作物理 `kGrid` 很容易误读负 k 分支。
- 修复建议：优先返回标准有符号 Nyquist k 网格并同步 `fftshift` 功率列。若必须保留 raw DFT 顺序，则把字段命名为 `kRawBins`，另返回有符号 `kGrid` 和排列索引。

### P3-06：Case A 的 H 半格初相位重复计算了空间半格位移

- 位置：`run_fdtd.m:96-104`
- 实际：`xHA=xA+dxA/2` 已把 H 放到空间半格，但 `Hhalf0A` 又额外乘以 `exp(i*k0A*dxA/2)`；正确的连续平面波初始化只需再乘时间错位 `exp(i*omega0A*dtA/2)`。
- 影响：默认参数多出相位 `0.0471239 rad`，对应理想平面波分解中约 `5.55e-4` 的反向功率。影响不大，但与“One-way wave packet”声明不完全一致。
- 修复建议：删除额外的 `k0A*dxA/2`，时间半步最好使用满足 Yee 数值色散的 `omegaYee`；并用均匀静态介质测试前/后向功率比。

## 4. 对 FDTD“结果很怪”的专项判读

实际导出并检查三张默认图后，可将异常分成“物理上预期”和“实现/验收问题”两类：

- **Case A 在每个半周期发生 E 的阶跃，主要是预期物理现象。** 内核推进并保持连续的是 `D/B`；当 `ε` 从 4 突变为 1 时，`E=D/ε` 会瞬时放大约 4 倍，反向切换时缩小。这解释了探针曲线在 `t/T=0.5,1,1.5,...` 的跳变，不应把这些跳变本身判为数值发散。Case A 仍有 P3-06 的小反向波初始化误差。
- **Case B 在动量带隙附近出现竖向宽带亮柱，部分属于有限时间 FFT 对增长/衰减模的预期表现。** 带隙中 `Im(ω) != 0`，指数包络的有限时间实频 FFT 本来就会展宽，不能恢复复频率虚部；但 P1-09 的噪声列放大和 P2-08 的边界阶跃源会进一步加重亮柱，修复前不能只凭图形判断谱权重。
- **Case C 的大色块和近乎全亮不是一张可用于科研判读的能带图。** 其 `32 x 8` 绘图网格中，32 个纵向点包含 4 倍零填充，真实分辨率仍只有 `π/4`；横向只有 8 个独立 k。再叠加逐列归一化和周期源阶跃，D-FFT 动态范围只有 `2.137 dB`，改用 E-FFT 也只有 `2.153 dB`，均看不到稳定脊线。它最多证明旧的双折叠代码路径执行过，不能证明有限样品电场能带正确。

## 5. 尚未定性为当前版本已复现 bug 的风险

以下项目应在修复时一并处理或验证，但本次不把它们记作 R2026a 已复现故障：

1. `run_pwe.m:131-147` 用 TMM 目标值贪心挑选最近的 PWE 分支，再用相同 TMM 计算误差，独立性不足；它也没有误差断言，候选少于两支时没有保护。建议先按 `m0Weight + 跨 k 连续性` 独立选 PWE 主带，再进行两分支全局匹配并设置明确阈值。
2. `tmm_bands.m:71-75` 在强增益单周期矩阵上直接 `eig(U)`，极小的互易乘子可能被舍入为 0。建议检查 `det(U)` 和有限性，并利用 `det(U)=1` 由稳定的大根恢复小根。
3. PWE 保留 `eig` 的逐点任意顺序，TMM 只在每个 k 点独立排序；二者都没有跨扫描点的分支身份追踪。若下游把结果的每一行当成同一条连续能带，应增加重叠度/连续性追踪或明确当前契约。
4. `run_tmm.m:105` 使用 `sgtitle(tl,...)`。R2026a 实跑成功，但 [MathWorks 的 `sgtitle` 文档](https://www.mathworks.com/help/matlab/ref/sgtitle.html)对 `target` 只列 Figure/Panel/Tab；最低版本 R2020a 尚未实跑。建议无损替换为从 R2019b 起明确支持 TiledChartLayout 的 `title(tl,...)`。

## 6. 建议修复顺序

1. **先改 FDTD 验收架构**：落实 P0-01/P0-02，定义有限样品、背景区、实验式宽带源、样品内 E 测量 ROI、空间/时间窗和有限尺寸收敛；旧周期算例若保留，只能作为内部底层回归，并应以 E 为主观测量、D 为附加诊断。
2. **再修科学正确性和安全性**：P1-01 至 P1-11，尤其是 CFL、重复端点、奇偶折叠、有效 k 列屏蔽、有限样品空间窗和验收假阳性。
3. **补齐公开契约**：`TMM.U`、PWE `mode='k'` 权重、本征矢语义、完整 BZ、TMM/材料输入验证。
4. **统一边界与返回形状**：有符号 k、ridge 方向、递增 x、海绵参数、重复配置字段。
5. **重新做一次性回归**：回归脚本可在发布过程或临时目录运行，按 V3 计划无需把 `tests/` 永久留在发布树中。

## 7. 3.0.1 重新验收门槛

以下条件全部满足后再判定通过：

- 三个入口在 MATLAB R2020a 和当前 MATLAB 版本均从仓库根目录直接完成。
- 均匀介质 PWE/TMM 解析误差满足设定容差；二元时间晶体 PWE/TMM 的实部、增长/衰减支和带隙边界分别比较，而不是只报告贪心匹配后的中位误差。
- FDTD 能带算例包含明确的有限材料样品、背景介质、样品界面、物理归一化的实验式宽带源、外部海绵和样品内部测量 ROI。固定样品/源/ROI，仅把背景 buffer 加倍或改变海绵参数后，目标响应在预注册容差内不变；无样品 reference 必须证明入射谱覆盖所有纳入统计的区域并量化外边界反射，或证明选定时间窗在回波返回前结束。
- 能带重建默认且验收强制使用 `E(x,t)`。报告必须写明背景 `ε/μ`、样品起止/长度/termination、调制启动时刻与相位、源位置/波形、ROI 起止、原生采样率、空间/时间窗及 ENBW、DFT bin spacing、有效分辨率和 active-k 判据；`D` 只允许作为附加诊断。raw signed 响应谱与可选折叠谱均须保留。
- 至少使用三种有限样品长度、两种记录时长和两种中央 ROI/空间窗，并分别完成“固定中央 ROI”和“随长度扩大 ROI”两类收敛；在共同 signed-k 网格上按 `1/L` 外推 ridge/线宽，把 Fabry-Pérot、边界模和 bulk 候选分开。有限样品容差不能机械照搬旧周期 `0.10/0.30`，应依据空间/时间窗有效线宽、`|vg|Δk_eff`、源带宽和 `1/L` 外推不确定度预注册，再将外推结果与理论比较。
- 完成 `dx/dt` 网格收敛，保持样品 termination、调制相位、源距和时间门一致；左右入射或多源位置应覆盖非互易/暗模，只有 reference 源谱高于噪声阈值的 ridge 才参与误差统计。
- 理论对照分开定义：有限时间晶体 Case B 的稳定 E-FFT ridge 对 TMM，有限时空晶体 Case C 对 PWE；ridge 必须先按源支持、显著度和跨长度连续性独立提取，不能用理论目标值反选最近峰。
- FDTD 对窄时域高速区、H 半格低 `μ`、负/零/NaN `maxWaveSpeed`、零材料和非法 `recordEvery` 均正确拒绝。
- 有限样品 ROI 强制使用 endpoint-free 网格；“endpoint-free 与 repeated-endpoint 等价”只作为旧周期 FFT 工具所承诺兼容路径的单元回归，不得自动删除有限样品最后一个真实测点。
- 有限样品的时间窗使用无重复端点、整数个调制周期；FFT kernel 另行覆盖奇偶周期数、奇偶每周期采样数和 `zeroPaddingFactor=1/4`。需要空间折叠的内部 ROI 跨整数个原胞，并覆盖 1、2、3、8 个周期的 kernel 回归。
- 纯 `±k` 模态在 no-fold 模式下返回正确的有符号 k；未激发列不会产生 0 dB 伪脊线。
- `result.U`、两种 PWE 模式的 `m0Weight`、ridge 形状与函数头文档完全一致。
- 原周期边界回归可继续满足 `0.10/0.30` 阈值，但应以 E 为主观测量、D 仅作附加诊断；控制台必须明确它只是底层数值回归，不能替代有限样品 E-FFT 验收，也不能参与公开算例的总通过判定。
- 修复后重新执行 Code Analyzer 和 Git 跟踪树检查，确认没有旧依赖、缓存或意外生成文件。

## 8. PWE 专项修复记录（2026-08-30）

本节记录用户对 PWE 最终用途的进一步收窄；它优先于上文 V3.0 历史验收中关于一般时空晶体、双模式、权重和 TMM 交叉图的旧要求。

- PWE 现在只处理空间均匀、时间周期、`mu_r=1` 的光学时间晶体。`run_pwe.m` 顶部集中设置方波/正弦调制、介电常数范围、占空比或相位、周期、Fourier 截断、采样数、`k/Omega` 范围以及实部的显示纵轴范围。
- `run_pwe.m` 只调用 `pwe_fourier` 和 `pwe_bands`，只绘制 `Re(omega)/Omega` 与 `Im(omega)/Omega`：实部由顶部纵轴范围显示完整谱中的副本，虚部按实部最接近 `Omega/4` 的原规则只绘制两条代表根。已删除一般时空晶体示例、TMM 调用、误差验收、权重着色、保存块和所有辅助分支追踪函数。
- `pwe_fourier(epsFun,pweCfg)` 的最小配置只含 `T/Mtime/Nt`；仍保留 endpoint-free 采样、`Nt >= 4*Mtime+1`、完整 Fourier 差阶、Hermitian/正定检查和正介电常数约束。
- `pwe_bands(fourier,kScan)` 只求固定 `k -> omega`。输出严格缩减为 `result.k` 与 `result.omega`；不再返回额外谱字段、权重、本征矢、左本征矢、条件数或固定频率模式。`result.omega` 保留每个 k 点的全部原始 Floquet 本征值，不在求解器内选择分支或折回第一 Floquet 区；`run_pwe.m` 通过顶部的实部纵轴范围选择显示的副本。
- 三个 PWE 文件总计从约 `957` 行降至 `286` 行，MATLAB R2026a Code Analyzer 均为 `0` 条提示。一次性、非入库对照中，默认方波相对精确二层 TMM 的两分支复频率配对误差中位数/最大值为 `3.361e-3/4.946e-2 Omega`；正弦相对 512 层中点 TMM 为 `5.745e-7/9.972e-6 Omega`。默认方波入口和正弦内核均完整运行。MATLAB R2020a 仍未在本机实跑。

## 9. TMM 专项修复记录（2026-08-30）

本节记录用户对 TMM 最终用途的进一步收窄；它优先于上文历史验收中关于多层介质、`result.U`、Floquet 乘子、半迹、带隙掩码和附加诊断图的旧要求。

- TMM 现在只处理空间均匀、无损、非色散且 `epsilon_r>0`、`mu_r>0` 的两层方波时间晶体。`run_tmm.m` 顶部集中设置两层 ε/μ、占空比、周期和 `k/Omega` 扫描。
- `run_tmm.m` 只调用 `tmm_bands`，只绘制 `Re(omega)/Omega` 与 `Im(omega)/Omega`。已删除半迹图、动量带隙着色、PWE/解析对比、控制台诊断和保存块。
- `tmm_bands(kScan,epsLayers,muLayers,durations)` 严格要求两个正持续时间层。它使用时间界面处连续的 `[D;B]` 状态和每层解析传播矩阵，不对时间离散；准频率由单周期矩阵的两个 Floquet 乘子主对数得到。
- 输出严格缩减为 `result.k` 与 `result.omega`。为避免强增长区直接求特征值时丢失很小的互易乘子，代码先稳定选取模较大的二次方程根，再以行列式除法恢复另一根；零、负、复数或非有限材料参数均被拒绝。
- 当时的 `run_fdtd.m` 曾同步解除对 `halfTrace` 的依赖；该旧入口现已由下节记录的两个独立 FDTD 脚本完全替代。
- `tmm_bands.m` 与 `run_tmm.m` 合计从约 `335` 行降至 `152` 行。MATLAB R2026a Code Analyzer 对两个 TMM 文件均为 `0` 条提示；均匀介质解析色散最大误差为 `2.871e-16`，默认方波动量带隙中增长/衰减支的共轭误差为 `7.068e-17 Omega`，默认入口可完整运行。MATLAB R2020a 仍未在本机实跑。

## 10. FDTD/FFT 专项重构记录（2026-08-30）

本节记录用户对 FDTD 最终用途的收窄；它优先于上文关于单一 `run_fdtd.m`、三个 Case、软源、理论叠加、ridge、权重、1/L 外推和多面板验收图的历史要求。

- 原 `run_fdtd.m` 已移除，替换为两个用途和数据流均独立的入口。`run_fdtd_field.m` 只计算初值激发的有限空间样品并绘制一张 `|E(x,t)|`；`run_fdtd_fft.m` 不读取前者的场数据，而是遍历用户指定的 k，绘制一张有限时间原胞采样得到的 Floquet 能带图。两者不是必须依次执行的“前后处理”关系。
- 场分布入口的几何为“海绵 | 静态背景 | 时间调制样品 | 静态背景 | 海绵”。`sampleCellCount*cellSize` 只表示有限样品的几何长度；纯时间晶体没有空间晶格，因此该长度单元不是空间晶格常数。用户直接设置高斯波包中心 `kCenterNormalized`，程序不再要求二选一“体带/k-gap”，具体区域由当前材料色散决定。
- 场分布入口支持方波和正弦介电调制。方波把 t=0 放在时间层内部，并把每个突变严格对齐到整数 FDTD 节点；初始 E/H 使用局部均匀介质的 Yee 色散、阻抗和半时间步相位近似。代码显式限制中心 k 及高斯主谱位于空间 Nyquist 区，并要求包络在有限样品端面前充分衰减。
- `fdtd1d.m` 只保留开放海绵边界、复 E/H 初值、D/B-Yee 推进、严格正材料检查、全时空 CFL 审计和时间突变中心修正。软源、周期边界及 D/H 全历史均已删除；输出只含 `x/t/E/dx/dt/courant`。海绵阻尼采用 `exp(-sigma*dt)` 连续时间率，避免改变 dt 时同时改变单位物理时间内的吸收；它仍不是 PML，必须用背景宽度、吸收层参数和仿真时间做边界回波收敛。
- 能带入口对每个守恒实 k 独立推进一个空间 Fourier 分量，空间差分采用与 FDTD 一致的 `kYee=2*sin(k*dx/2)/dx`。每个 k 用两组等离散能量的独立 D/B 初态张成 Maxwell 状态空间，以降低单一初态漏支风险；这只是高效实现逐 k 激发，不把有限时间采样改写成无限时间本征值求解。
- 对每个 k 只记录 `temporalCellCount` 个样本：在 `t=pT`、`p=0,...,N_T-1` 的同一调制相位采样 E，然后沿有限原胞序列使用 `ifft`。在 `exp(i*k*x-i*omega*t)` 约定下，`ifft` 直接给出正 omega；每 T 一次的同相位采样会把相差整数倍 Ω 的 Floquet 副本自动映射到第一时间 Floquet 区 `[-Omega/2,Omega/2)`。这里不进行空间 FFT、有限样品 ROI、空间窗或空间波数折叠。
- 时间序列使用周期型 Hann 窗。有限 `N_T` 的原生分辨率约为 `Delta[Re(omega)/Omega]=1/N_T`，Hann 主瓣还会使峰展宽；零填充只细化显示坐标。由于扫描中的每个 k 都被两组完整初态直接激发，谱按 k 列独立归一化以显示峰位，但不同 k 列的颜色强度不再具有可比较的增长或响应权重意义。
- `fdtd_fft_bands.m` 的输出严格缩减为 `kNormalized/omegaNormalized/spectrumDb`。普通有限序列 FFT 只能重建 `Re(omega)`；k-gap 中指数增长/衰减会引起有限窗展宽和权重偏置，不能把线宽解释为 `Im(omega)`。科研使用必须检查 dx、dt、`temporalCellCount` 和窗口收敛，强增长时还要防止有限精度溢出。
- 四个 FDTD/FFT 文件现共 `887` 行，MATLAB R2026a Code Analyzer 对四个文件均为 `0` 条提示。默认场入口只生成一张图，复电场历史为 `601 x 1120`、Courant 数为 `0.2`，记录准确到 `12T`；默认能带入口也只生成一张图，使用 64 个有限同相位样本，得到 `256 x 301` 的零填充显示谱。均匀介质测试在解析 Yee 色散的 `omega/Omega=±0.25` 两个 bin 均得到 0 dB 峰；方波通带对精确 TMM 的循环频率峰位最大误差为 `1.153e-3 Omega`，`P(-k,-omega)=P(k,omega)` 的最大 dB 差为 `2.465e-11`，正弦调制有限采样路径也通过对称性与有限性检查。奇数采样点频轴、时间界面 D 连续、CFL 拒绝、空间 Nyquist 拒绝及近重复界面拒绝均通过；MATLAB R2020a 仍未在本机实跑。

## 11. 论文复现反馈驱动的 FDTD 修复（2026-08-30）

本节记录根据 `topology_aspect/TEMPLATE_CODE_FEEDBACK.md` 执行的后续修复；它更新并覆盖第 10 节关于边界、场输出和最小返回字段的旧描述。

- `fdtd1d.m` 现在显式区分 `fixed-distant` 与 `sponge`。前者是无阻尼固定远端截断，后者仍只是固定端点前的指数海绵而非 PML；返回结构记录边界类型、海绵参数、全域范围、最大波速和全域穿越时间，不再笼统称作严格开放边界。
- `run_fdtd_field.m` 默认采用 `fixed-distant`，并以“最大传播距离 + 初始高斯六倍宽度”检查主场和可分辨尾场在仿真结束前不能到达端点。选择海绵时会明确警告：k-gap 可能指数放大海绵散射出的极小附加 k 分量，必须做边界距离、宽度和强度收敛。
- 场入口新增有限 `modulationStartTime/modulationEndTime`、方波开启相位、`sample/global` 调制区域。方波开启、关闭和内部跳变由节点左右材料值自动识别，并继续严格对齐整数 FDTD 节点。
- 场入口新增 `normalized/um-fs` 单位、`normalized-k/wavelength` 激发输入和明确定义的强度 FWHM；H 半格初值同时修正载波半时间相位与高斯包络半步位移。
- `fdtd1d.m` 可通过 `recordField='E'|'D'` 只记录一个观测量，通过 `recordSpatialIndices` 只记录空间 ROI，并用 `recordPrecision` 选择历史数组精度；内部 D/B 推进始终保持双精度。E 在时间节点采用材料回调的右连续值，D 直接记录连续推进变量。
- 对取值范围解析已知的简单材料，可同时提供 `certifiedMinimumEpsilon/certifiedMinimumMu` 跳过昂贵的全时空 CFL 预扫描。内核在每次实际材料求值时仍核查认证下界；未提供下界时继续执行原有的全 E/H Yee 网格、全时间严格审计。
- `fdtd_fft_bands.m` 的主图数据仍为逐 k 归一化有限窗实准频率谱，但新增未归一化列功率、后半时间窗主导增长指数、Courant 数、原胞数、FFT 点数、原生频率间隔和 Hann 窗增益。可选 `returnTemporalSamples=true` 才返回两组初态的完整同相位复电场样本。
- 主导增长指数来自两组完备初态总振幅后半窗的对数线性拟合。它在足够长的 k-gap 记录中近似正增长支 `max Im(omega*T)`；通带的小残差只作诊断，不能替代复 Floquet 两支求解，也不能把 FFT 谱宽解释成虚部。
- 论文复现入口已改为直接记录 D 的单精度空间 ROI，不再保存全域双精度 E 后重复计算 `D=epsilon*E`。原先与根目录 SHA-256 完全一致的 9 个模板副本随后从 `topology_aspect/` 删除；三个复现入口通过 `use_root_templates.m` 把根目录置于搜索路径首位，以 `which()` 强制验证实际求解器，并把绝对路径写入返回结构体 `templateSource`。
- MATLAB R2026a 回归中，论文图 2 的 D-only ROI 为 `2501 x 3251` single，Courant 数 `0.374741`；通带最大对数幅度 `0.485258`，k-gap FDTD/TMM 的 60 周期对数增长为 `29.7926/29.8726`。FDTD–FFT 的 `782` 个稳定通带峰位误差中位数/最大值为 `0.00223/0.0375 Omega`；`111` 个 Yee-TMM 带隙增长样本的 `|Delta Im(omega*T)|` 中位数/最大值为 `0.00114/0.0482`。

## 12. FDTD–FFT 实验式波包/探针重构（2026-08-31）

本节更新并覆盖第 10–11 节中“逐 k 直接推进单个空间 Fourier 分量、每周期同相位采样”的 FFT 数据流。当前要求是：每个 k 都必须对应一次有限空间高斯初值 FDTD，FFT 只能读取该仿真的固定空间 E 探针时间序列。

- `run_fdtd_fft.m` 现在为每个扫描中心 `k_c` 构造与 `run_fdtd_field.m` 相同约定的强度-FWHM 高斯 E/H 初值，包括传播方向、Yee 离散载频、H 半时间相位和包络半步位移；随后逐列调用根目录 `fdtd1d.m`，不再使用 2×Nk 的 Fourier 模式状态更新。
- 几何为固定远端/海绵、静态背景和有限时间调制样品。高斯包络在样品端面必须低于显式阈值；固定远端还按最大传播距离与该阈值对应的尾场半径检查回波安全。扫描期间样品、网格、时间窗和固定 E 探针位置保持不变，只改变载波中心 k。
- 多个样品内部探针分别记录完整时间步上的复 E(t)，先各自做 FFT，再非相干相加功率。默认还为一个代表性 k 保存样品内部完整 E(x,t)，从而可以直接审计能带列所来自的时空场。`fdtd_fft_bands.m` 已改为纯后处理函数，签名为 `fdtd_fft_bands(probeSignals,time,kScan,cfg)`。
- 时间分析窗采用 `[start,end)` 的无重复端点均匀采样，严格覆盖整数个调制周期。周期型 Hann 窗后先保留完整物理频率谱，再在未移位 bin 上按模显式累加相差整数 Ω 的功率；奇数/偶数周期和零填充均使用同一索引约定。
- 输出保留 raw 物理频率功率、折叠功率、逐列显示谱、未归一化列功率、有效列掩码、探针数、分析窗、原生分辨率和 Hann 窗增益/ENBW。横轴明确为有限高斯源中心 `k_c`；谱是源、有限包络、有限样品、探针位置和有限时间窗共同加权的响应，不能称为无限体精确本征谱，谱宽也不解释为 `Im(omega)`。
- MATLAB R2024b 默认端到端回归完成 81 次有限空间仿真，5 个探针数据尺寸为 `1601 x 5 x 81`，代表场为 `1601 x 864`，折叠谱为 `64 x 81`，Courant 数为 `0.12`，所有有效列和谱值均有限。另用 7 个周期的合成探针信号验证了正频率方向、整数 Ω 副本折叠、奇数周期索引与 4 倍零填充；两个目标峰分别精确落在 `0.25 Omega` 和 `-2/7 Omega`。

## 13. topology_aspect 对新 FDTD–FFT 的论文参数验证（2026-08-31）

- 逐 k 高斯初值、Yee H 半步构造、`fdtd1d` 调用和探针提取已抽为根目录共享函数 `fdtd_gaussian_k_scan.m`；`run_fdtd_fft.m` 与 `topology_aspect/reproduce_fdtd_band.m` 均调用它，避免论文入口复制并漂移出另一套扫描实现。
- 论文只规定 `epsilon=3/1`、`T=2 fs` 和能带坐标，没有规定 FFT 波包、固定探针、网格或有限空间长度。复现入口把默认数值假设显式登记为：`0<=k/k0<=5` 的 101 点、16 个周期、每周期 100 步、光行时 dx=`0.04 fs`、有限样品长度 `192 fs`、强度 FWHM `24 fs` 和 5 个固定 E 探针；可选 overrides 用于后续收敛。
- 默认 MATLAB R2024b 运行得到 `1601 x 5 x 101` 的复探针数据、`64 x 101` 折叠谱和 Courant 数 `0.5`。对 Yee-TMM 稳定通带中高于 `-20 dB` 的 137 个局部峰，误差中位数/P90/最大值为 `0.00565/0.03466/0.09194 Omega`；放宽到 `-35 dB` 的 148 个峰后为 `0.00612/0.02944/0.09194 Omega`。
- 复现只把 Yee-TMM 用于后台匹配实际局部谱峰，理论曲线不叠加主图；匹配明细、raw/folded 功率、探针 E(t)、代表性 E(x,t) 和模板绝对路径均保存在返回结构体。输出图为 `topology_aspect/fdtd_band_validation.png`，完整 MAT 数据约 70 MB 并由 `.gitignore` 排除。
- 固定探针波包响应不再输出 bulk 增长率。k-gap 虚部应继续由 TMM/PWE 或固定 k 单模 Floquet 方法验证；有限波包离开探针、空间包络和多模拍频使简单后半窗斜率不具备同一物理含义。
