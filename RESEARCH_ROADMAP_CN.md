# 从代码包到课题：建议路线

## 推荐主线：严格时间拓扑与非理想性

这是最适合现有代码基础、也最贴近你拓扑光子学背景的一条线。

### 里程碑 1：理想二元 PTC

已有基线：

- `demo03`：TMM/PWE 复能带；
- `demo09`：有限周期输出幅度和复相位；
- `demo10`：AB/BA 时间畴壁的增长—衰减模匹配；
- `demo12`：方波 PWE 截断收敛。

新增研究任务：

1. 固定准频率 $\Omega_F$ 求一组 $k_m(\Omega_F)$；
2. 重构一周期时间 Bloch 模并保持分支连续；
3. 在明确反演中心的时间单胞上计算时间 Zak Wilson 环；
4. 用反演本征值给出独立的 $0/\pi$ 判据；
5. 证明 AB/BA 同谱、拓扑指标不同，并与有限晶体散射相位和 `demo10` 畴壁根对应。

必须做的反证测试：

- 闭合带隙后拓扑指标和畴壁态如何消失；
- 破坏时间反演/镜面对称后 Zak 相是否失去量子化；
- 改变单胞原点后数值如何变换；
- 接近异常点时单带 Wilson 环何时失效。

### 里程碑 2：时间色散

把状态从 $[D,B]$ 扩为 Maxwell–Lorentz/Drude 辅助变量，比较三种结果：

- 瞬时 $\epsilon(t)$ 模型；
- 时变等离子频率；
- 时变共振频率/振子强度。

研究问题：

- 色散是否移动或关闭动量带隙？
- 时间拓扑指标在扩大后的非 Hermitian Floquet 状态空间中如何定义？
- 有限开关时间和因果性是否改变时间界面相位？

验证要求：

- 静态极限恢复解析色散；
- 调制深度趋零时恢复未调制极点；
- TMM/ADE/Floquet 单周期矩阵至少两种方法交叉验证。

### 里程碑 3：实验数值孪生

选择 2015/2016 传输线或 2025 非合成 PTC 实验，建立：

- 离散 LC 单胞；
- 时变电容/开关状态；
- 电阻、辐射和测量端口；
- 实际驱动波形与有限上升沿；
- 电压/电流到论文观测量的同一后处理。

此时才能定量比较实验的 $k$ 带隙、增长率、时间反射系数和畴壁态位置。

## 可并行的四个独立课题

### A. 时间界面相干控制

从 `demo11` 出发，研究宽带脉冲、相位噪声、有限开关速度和不同微观跃迁律。可加入多时间界面，做目标输出通道的梯度优化。

### B. 一般 ST-PWE 的复谱与异常点

从 `demo01`/`demo02` 出发，扫描调制速度、深度和相位：

- 自动追踪复频率分支；
- 用最小邻带间隔和双正交自正交性寻找异常点；
- 用 FDTD 验证增长率和侧带组成；
- 区分频率带隙与动量带隙。

### C. 缺陷、无序与有限周期 PTC

从 `temporal_finite_crystal_response` 出发，在有限时间序列中加入：

- 单层持续时间缺陷；
- 随机持续时间和介电常数；
- 相关无序；
- 多个时间畴壁。

输出不只看峰值，还应统计复散射相位、增益分布、局域长度和对输入状态的敏感性。

### D. 平台专用模型

建立相互独立的目录：

```text
models/
  transmission_line/
  metasurface/
  synthetic_mesh/
  conformal_boundaries/
  dispersive_ptc/
```

每个目录都有自己的状态方程和验证，不共享不适用的边界条件；只共享参数记录、扫描、绘图和拓扑诊断接口。

## 一个结果是否接近论文级的检查表

- 有明确的新问题，而非只换参数；
- 有基线解析式或精确矩阵；
- 有至少一种独立数值方法；
- 有收敛曲线与误差阈值；
- 有对称性、能隙和边界条件说明；
- 有失败区域和反例；
- 有原始数据、参数结构和可重跑入口；
- 物理结论能由一个可测量量支持。

## 不建议的起步方式

- 直接把 `demo08` 的 $C=1$ 写成“时空介质 Chern 数”；
- 把空间 Zak 相当作时间 Zak 相；
- 对所有时间界面默认 D/B 连续而不说明开关机制；
- 用方波 PWE 的一个截断结果代替 TMM；
- 只画有限时间放大，不区分本征增长、输入投影和数值不稳定；
- 在连续介质模型上声称复现传输线或超表面实验。

